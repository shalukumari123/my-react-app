trigger:
- main   # or your branch

pool:
  vmImage: 'ubuntu-latest'

variables:
  imageName: 'yourdockerhubusername/react-app'

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'dockerhub-service-connection'   # service connection name
    repository: '$(imageName)'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
    tags: |
      $(Build.BuildId)
      latest
